<h1>Teams</h1>
<% if @teams.blank? %>
  <h1>No teams yet!</h1>
  <%= link_to :new_team, new_team_path %>
<% else %>
  <% @teams.each do |team| %>
    <div>
      <%= team.name %>
      <%= team.strokes %>
    </div>
  <% end %>
<% end %>
