<% if @users.blank? %>
  <h1>No users yet!</h1>
  <%= link_to :create_user, new_user_path %>
<% else %>
  <% @users.each do |user| %>
    <%= user.name %>
    <%= user.phone_number %>
    <%= user.email %>
  <% end %>
<% end %>

<%= link_to 'Add more users', new_user_path %>

